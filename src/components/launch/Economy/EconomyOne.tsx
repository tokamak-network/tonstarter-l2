import { Flex, Text } from "@chakra-ui/react";
import Details from "./Details";
const EconomyOne = () => {
    return <Flex flexDir={'column'}>one
        <Details/>
    </Flex>
}

export default EconomyOne